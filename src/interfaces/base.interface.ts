export interface MetaInterface {
  totalItems : number,
  totalCount : number,
  itemsPerPages : number,
  totalPages : number,
  currentPage : number,
}

export interface LinksInterface {
  first : string,
  previous : string,
  next : string,
  last : string,
}